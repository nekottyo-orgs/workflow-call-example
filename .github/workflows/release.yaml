name: release
on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    types:
      - labeled

jobs:
  release:
    uses: nekottyo-org/auto-tag-action-example/.github/workflows/release.yaml@main

  release-check:
    uses: nekottyo-org/auto-tag-action-example/.github/workflows/release-check.yaml@main
